#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(данныеВыбора, __, стандартнаяОбработка)
    стандартнаяОбработка = Ложь;
    списокДоступныхЗначений = Новый СписокЗначений;
    Для Каждого значение Из Перечисления.ВидыОперацийРасходаДенег Цикл
        Если значение <> Перечисления.ВидыОперацийРасходаДенег.ПрочийРасход Тогда
            списокДоступныхЗначений.Добавить(значение);
        КонецЕсли;
    КонецЦикла;
    данныеВыбора = списокДоступныхЗначений;
КонецПроцедуры

#КонецОбласти // ОбработчикиСобытий
